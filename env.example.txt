# Environment configuration for the application
# Copy this file to .env and fill in the required values.

# === RapidAPI Configuration ===
# Replace with your RapidAPI key. This is required to authenticate requests to the Kohls API.
REACT_APP_X_RAPIDAPI_KEY=

# Replace with the host URL for the Kohls API (provided by RapidAPI).
REACT_APP_X_RAPIDAPI_HOST=kohls.p.rapidapi.com

# === Firebase Configuration ===
# Firebase API Key: Used to authenticate your app with Firebase services.
REACT_APP_API_KEY_FIREBASE=

# Firebase Auth Domain: Domain used for Firebase authentication (e.g., your-app.firebaseapp.com).
REACT_APP_AUTH_DOMAIN_FIREBASE=

# Firebase Project ID: The unique identifier for your Firebase project.
REACT_APP_PROJECT_ID_FIREBASE=

# Firebase Storage Bucket: Used for storing files in Firebase (e.g., your-app.appspot.com).
REACT_APP_STORAGE_BUCKET_FIREBASE=

# Firebase Messaging Sender ID: Used for Firebase Cloud Messaging (for push notifications).
REACT_APP_MESSAGING_SENDER_ID_FIREBASE=

# Firebase App ID: A unique identifier for your Firebase application.
REACT_APP_APP_ID_FIREBASE=

# === Application API URL ===
# The base URL of your backend API (e.g., https://api.example.com).
# Update this to match the URL of your backend server.
REACT_APP_URL_API=